# Copy this file to .env. The docker-compose.yml uses these same values.
# Puerto 5433 (no 5432) para no chocar con un Supabase/Postgres local si lo tienes.
DATABASE_URL=postgres://cata:cata_dev@localhost:5433/cata_learning
